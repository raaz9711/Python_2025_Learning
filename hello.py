def hello_world():
    print("Hello, world! This is a simple Python script.")
    print("What is your name?")
    name = input("Enter your name: ")
    print(f"Hello, {name}! Welcome to the Python world.")

if __name__ == "__main__":
    hello_world()
else:
    print("This module can be imported without executing the main function.")
    # This allows the module to be used in other scripts without running the main function.
    # You can import this module in another script and call hello_world() as needed.    # Example:
    # from hello import hello_world
    # hello_world()
    # This is useful for testing or reusing the function in other parts of your code.
    # The main function will only run when this script is executed directly, not when imported.
    # This is a common practice in Python to allow for both direct execution and importation.    # This way, you can keep your code organized and reusable.    # This is a common practice in Python to allow for both direct execution and importation.
    # This way, you can keep your code organized and reusable.  # This is a common practice in Python to allow for both direct execution and importation.    # This way, you can keep your code organized and reusable.
    # This is a common practice in Python to allow for both direct execution and importation.   # This way, you can keep your code organized and reusable.
    # This is a common          
    